_name = 5 # valid
name1 = 5 # valid
# a variable name can contain underscores, letters, digits only
# it cannot be started by a digit
# it cannot contain a symbol
# it cannot contain spaces
# it cannot contain a keyword/reserved word