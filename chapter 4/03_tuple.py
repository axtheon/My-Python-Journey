# A tuple is same as a list but we cant change the value of a tuple.
# The main point of a tuple is that it cant be changed.
a = ("Apple", "Orange", "Abdullah", "Saeed", "Khan", 5, False, True, 38.0499)
# A list usually includes '[]' these Brackets but a tuple uses '()' these brackets.
# These are the identification of a tuple.
print(a)
print(type(a))
